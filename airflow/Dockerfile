FROM bitnami/airflow:latest
MAINTAINER sukarmagurung20@gmail.com
ENV AIRFLOW_FERNET_KEY=my_fernet_key
ENV AIRFLOW_SECRET_KEY=my_secret_key
ENV AIRFLOW_DATABASE_USERNAME=bn_airflow
ENV AIRFLOW_DATABASE_PASSWORD=bitnami1
ENV AIRFLOW_DATABASE_NAME=bitnami_airflow
ENV AIRFLOW_EXECUTOR=CeleryExecutor
ENV AIRFLOW_WEBSERVER_AUTHENTICATE=True
ENV AIRFLOW_WEBSERVER_AUTH_BACKEND=airflow.auth.backends.password_auth
ENV AIRFLOW_WEBSERVER_DEFAULT_UI_USER=admin
ENV AIRFLOW_WEBSERVER_DEFAULT_UI_PASSWORD=admin


